name: map_mapper_generator
description: Generates code that maps (converts) between instances of business classes to Map<String, dynamic> and back
homepage: https://github.com/squarealfa/dart_framework/tree/main/map_mapper/map_mapper_generator
repository: https://github.com/squarealfa/dart_framework
version: 2.0.0

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  map_mapper_annotations: ^2.0.0
  source_gen: ^1.0.0
  decimal: ^1.0.0
  squarealfa_generators_common: ^2.0.0
  analyzer: ^2.0.0
  build: ^2.0.0

dev_dependencies:
  lints: ^1.0.1
