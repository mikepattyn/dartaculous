name: proto_generator_test
description: A starting point for Dart libraries or applications.
environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  decimal: ^2.3.0
  
  proto_generator:
    path: ../proto_generator
  proto_annotations:
    path: ../proto_annotations
  squarealfa_generators_common:
    path: ../../generators_common
  squarealfa_common_types:
    path: ../../common_types
  grpc: ^3.0.2
  yaml: ^3.1.1

dev_dependencies:
  build_runner: ^2.1.4
  build_verify: ^3.0.0
  lints: ^2.0.0
  test: ^1.17.0

dependency_overrides:
  proto_annotations:
    path: ../proto_annotations
  squarealfa_generators_common:
    path: ../../generators_common
  squarealfa_common_types:
    path: ../../common_types
  proto_generator:
    path: ../../proto_mapper/proto_generator
