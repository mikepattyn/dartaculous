

dependency_overrides:
  proto_annotations:
    git:
      url: git@gitlab.com:squarealfa/dart_framework.git
      ref: protomember
      path: proto_mapper/proto_annotations

  proto_generator:
    git:
      url: git@gitlab.com:squarealfa/dart_framework.git
      ref: protomember
      path: proto_mapper/proto_generator

  squarealfa_generators_common:
    git:
      url: git@gitlab.com:squarealfa/dart_framework.git
      ref: protomember
      path: generators_common

  squarealfa_common_types:
    git:
      url: git@gitlab.com:squarealfa/dart_framework.git
      ref: protomember
      path: common_types

