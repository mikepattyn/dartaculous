name: proto_mapper_example
description: proto_annotations and proto_generator packages demonstration project
homepage: https://gitlab.com/squarealfa/dart_framework/-/tree/main/proto_mapper/example
repository: https://gitlab.com/squarealfa/dart_framework
version: 2.0.0

publish_to: none

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  protobuf: ^2.1.0
  proto_annotations: ^3.0.0-dev.1
dev_dependencies:
  lints: ^2.0.1
  test: ^1.14.4
  proto_generator: ^4.0.0-dev.6
  build_runner: ^2.1.2

