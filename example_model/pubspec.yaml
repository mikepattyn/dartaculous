name: example_model
description: Complete model demonstration project
homepage: https://gitlab.com/squarealfa/dart_framework/-/tree/main/proto_mapper/example
repository: https://gitlab.com/squarealfa/dart_framework
version: 1.0.0

publish_to: none

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  proto_annotations: ^2.1.1
  map_mapper_annotations: ^3.1.1
  

dev_dependencies:
  build_runner: ^2.1.2
  lints: ^2.0.0
  map_mapper_generator: ^3.4.1
  proto_generator: ^3.3.4
  test: ^1.21.1


dependency_overrides:
  proto_annotations:
    path: ../proto_mapper/proto_annotations
  proto_generator:
    path: ../proto_mapper/proto_generator
  map_mapper_generator:
    path: ../map_mapper/map_mapper_generator
  squarealfa_generators_common:
    path: ../generators_common
